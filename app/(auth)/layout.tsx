import { Children } from "react";

const AuthLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <div className="bg-pink-500 h-full">Login page</div>
    )

}

export default AuthLayout;
